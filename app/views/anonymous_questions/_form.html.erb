<%= form_for(@anonymous_question) do |f| %>
  <% if @anonymous_question.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@anonymous_question.errors.count, "error") %> prohibited this anonymous_question from being saved:</h2>

      <ul>
      <% @anonymous_question.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :question %><br>
    <%= f.text_field :question %>
  </div>
  <div class="field">
    <%= f.label :active %><br>
    <%= f.check_box :active %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
