<%= form_for :anonymous_question do |f| %>
  <% @questions.each do |question| %>
    <%= f.label question.question %>
    <%= f.text_area "anonymous_response[#{question.id}]" %>
  <% end %>

  <%= f.submit %>
<% end %>
